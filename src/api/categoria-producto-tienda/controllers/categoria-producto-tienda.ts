/**
 * categoria-producto-tienda controller
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController(
  'api::categoria-producto-tienda.categoria-producto-tienda'
);
